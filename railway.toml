[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "HOST=0.0.0.0 PORT=${PORT} node .next/standalone/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300

[build.env]
NIXPACKS_NODE_VERSION = "20"
